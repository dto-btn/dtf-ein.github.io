version: 2.1

jobs:

  # Checkout and build the site
  build:
    docker:
      - image: jekyll/builder:$JEKYLL_VERSION
    steps:
      - checkout
      - run: bundle install
      - run: jekyll build
      - save_cache:
          key: site-{{ .Environment.CIRCLE_SHA1 }}
          paths:
            - ~/project

  # Uses html-proofer to check the HTML generated by Jekyll
  test_html:
    docker:
      - image: 18fgsa/html-proofer:latest
    steps:
      - restore_cache:
          key: site-{{ .Environment.CIRCLE_SHA1 }}      
      - run: htmlproofer ~/project_site --empty-alt-ignore

workflows:
  version: 2.1
  build-test:
    jobs:
      - build
      - test_html:
          requires:
            - build   